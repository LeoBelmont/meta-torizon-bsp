#@TYPE: Machine
#@NAME: ConnectTech Rudi-NX (Jetson Orin NX / NGX010)
#@DESCRIPTION: ConnectTech Rudi-NX embedded system, Jetson Orin NX module, NVMe boot

require conf/machine/include/orin-nx.inc
require conf/machine/include/tegra-torizon.inc

# ConnectTech Rudi-NX carrier = the NGX010 device tree, booting from the
# onboard M.2 NVMe. All the Torizon behaviour (boot patch, kernel-arg
# inheritance, dense tegraflash tar, right-sized rootfs, no A/B, apparmor)
# comes from tegra-torizon.inc; only the carrier DTB + bootloader BCT/pinmux
# differ from an NVIDIA Orin NX devkit. Values taken from the CTI r39.2 BSP
# (conf/cti/orin-nx/rudi-nx/base.conf + cti-orin-nx.conf.common).
TNSPEC_BOOTDEV = "nvme0n1p1"

KERNEL_DEVICETREE = "tegra234-orin-nx-cti-NGX010.dtb"
PREFERRED_PROVIDER_virtual/dtb = "cti-rudi-nx-dtb"

TEGRA_FLASHVAR_PINMUX_CONFIG  = "tegra234-cti-orin-nx-rudi-mb1-bct-pinmux.dtsi"
TEGRA_FLASHVAR_PMC_CONFIG     = "tegra234-mb1-bct-padvoltage-p3767-dp-a03.dtsi"
TEGRA_FLASHVAR_MISC_CONFIG    = "tegra234-cti-orin-nx-mb2-bct-misc.dts"
TEGRA_FLASHVAR_SCR_CONFIG     = "tegra234-cti-orin-nx-mb2-bct-scr.dts"
TEGRA_FLASHVAR_GPIOINT_CONFIG = "tegra234-cti-orin-nx-mb1-bct-gpioint.dts"

OVERLAY_DTB_FILE = "L4TConfiguration.dtbo,tegra234-carveouts.dtbo,tegra-optee.dtbo,tegra234-orin-nx-cti-overlay.dtbo,tegra234-dcb-p3767-0000-hdmi.dtbo"

# The CTI carrier BCT/pinmux/DTB files are injected into the flash staging by
# the tegra-bootfiles bbappend (scoped :rudi-nx). The kernel device tree is a
# prebuilt CTI blob (not in the OOT kernel source), so it is supplied by the
# cti-rudi-nx-dtb recipe (PROVIDES virtual/dtb) instead of being built.
